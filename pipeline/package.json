{
  "name": "hiddenpod-pipeline",
  "version": "1.0.0",
  "description": "HiddenPod data ingestion pipeline — build step only, not shipped to browser",
  "private": true,
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0"
  },
  "scripts": {
    "discover": "node 01_discover.js",
    "ingest":   "node 02_ingest.js",
    "score":    "node 03_score.js",
    "assemble": "node 04_assemble.js"
  },
  "engines": {
    "node": ">=18"
  }
}
